<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugasakhirpember">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logoss"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logoss"
        android:supportsRtl="true"
        android:theme="@style/Theme.JagungkuSehat">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.HasilDiagnosaActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activities.DetailPenyakitActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>
